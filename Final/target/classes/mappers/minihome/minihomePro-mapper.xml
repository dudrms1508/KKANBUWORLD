<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.kh.world.minihome.model.mapper.MinihomeProMapper">
	
	<update id="updateIntro">
		UPDATE MINIHOME 
		SET
			INTRO = #{content}
		WHERE USER_NO = #{userNo} AND MINIHOME_NO = #{minihomeNo}
	</update>
	
	<update id="updateTitle">
		UPDATE MINIHOME 
		SET
			title = #{title}
		WHERE USER_NO = #{userNo} AND MINIHOME_NO = #{minihomeNo}
	</update>
</mapper>